name: Option 
description: This element is used to create a control representing an item within a <select> an <optgroup> or a <datalist> HTML5 element. 
twig: ./option.twig
category: base
subcategory: forms
atomic:
	patternType: atoms
	patternSubType: forms
data:
  i: 0
  option:
    - {class: base__option, value: value1, text: Value 1}
